Run Oblig1Oppgave1 first, as this is the math side of the task.
Then run TrialProject, the OpenGL version.

The program works by writing the data to a text file,
it writes it to this folder "c:/data/"

therefore, for the program to work, you will have to navigate to c:/
and create a folder named "data" (make sure to write it correctly, uncapitalized.)

OR

Change the Write and Read function of both programs to utilize a different existing folder. 

